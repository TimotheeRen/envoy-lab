apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: Lab
image: rancher/k3s:v1.37.0-k3s1
servers: 1
agents: 1
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters: [server:*]
      - arg: --disable=servicelb
        nodeFilters: [server:*]
  values:
    crds:
      enabled: false
