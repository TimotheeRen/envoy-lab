apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx
spec:
  rules:
    - backendRefs:
        - kind: Service
          name: app-service
          port: 8080
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
